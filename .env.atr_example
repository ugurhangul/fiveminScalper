# ============================================================
# ATR TRAILING STOP CONFIGURATION EXAMPLE
# ============================================================
# Copy these settings to your .env file to enable ATR-based trailing stops
#
# For detailed documentation, see: ATR_TRAILING_STOP_GUIDE.md
# ============================================================

# ============================================================
# BASIC TRAILING STOP SETTINGS (Required)
# ============================================================

# Enable trailing stop functionality
USE_TRAILING_STOP=true

# R:R ratio to activate trailing stop
# Position must reach this profit level before trailing begins
# Example: 1.5 means trailing activates when position is at 1.5× risk in profit
TRAILING_STOP_TRIGGER_RR=1.5

# ============================================================
# ATR TRAILING STOP SETTINGS
# ============================================================

# Enable ATR-based trailing (set to false to use fixed-distance trailing)
USE_ATR_TRAILING=true

# ATR calculation period (number of candles)
# Common values: 10, 14, 20
# Default: 14 (standard ATR period)
ATR_PERIOD=14

# ATR multiplier (distance from peak price)
# Higher = wider stops (more room for retracements)
# Lower = tighter stops (quicker profit-taking)
# Recommended range: 1.5 - 3.0
# Default: 2.0 (balanced approach)
ATR_MULTIPLIER=2.0

# Timeframe for ATR calculation
# Options: M5, M15, M30, H1, H4, D1
# Recommendation: Match your trading timeframe or use one level higher
# Default: H4 (for 4-hour strategy)
ATR_TIMEFRAME=H4

# ============================================================
# FIXED TRAILING STOP SETTINGS (Used when USE_ATR_TRAILING=false)
# ============================================================

# Fixed distance in points for trailing stop
# Only used when USE_ATR_TRAILING=false
TRAILING_STOP_DISTANCE=50.0

# ============================================================
# PRESET CONFIGURATIONS
# ============================================================

# CONSERVATIVE (Tight Stops - Quick Profit Taking)
# Uncomment to use:
# USE_ATR_TRAILING=true
# ATR_PERIOD=14
# ATR_MULTIPLIER=1.5
# ATR_TIMEFRAME=H1
# TRAILING_STOP_TRIGGER_RR=1.0

# BALANCED (Recommended for Most Strategies)
# Uncomment to use:
# USE_ATR_TRAILING=true
# ATR_PERIOD=14
# ATR_MULTIPLIER=2.0
# ATR_TIMEFRAME=H4
# TRAILING_STOP_TRIGGER_RR=1.5

# AGGRESSIVE (Wide Stops - Let Winners Run)
# Uncomment to use:
# USE_ATR_TRAILING=true
# ATR_PERIOD=14
# ATR_MULTIPLIER=3.0
# ATR_TIMEFRAME=H4
# TRAILING_STOP_TRIGGER_RR=2.0

# SCALPING (Very Tight Stops)
# Uncomment to use:
# USE_ATR_TRAILING=true
# ATR_PERIOD=10
# ATR_MULTIPLIER=1.0
# ATR_TIMEFRAME=M15
# TRAILING_STOP_TRIGGER_RR=0.5

# SWING TRADING (Wide Stops, Higher Timeframe)
# Uncomment to use:
# USE_ATR_TRAILING=true
# ATR_PERIOD=14
# ATR_MULTIPLIER=2.5
# ATR_TIMEFRAME=D1
# TRAILING_STOP_TRIGGER_RR=2.0

# ============================================================
# NOTES
# ============================================================
#
# 1. ATR MULTIPLIER GUIDE:
#    - 1.0-1.5: Very tight, suitable for scalping
#    - 2.0-2.5: Balanced, suitable for most strategies
#    - 3.0+: Wide, suitable for swing trading
#
# 2. TIMEFRAME SELECTION:
#    - Lower timeframes (M5, M15): More responsive, frequent updates
#    - Medium timeframes (H1, H4): Balanced, recommended
#    - Higher timeframes (D1): Less responsive, stable
#
# 3. TRIGGER R:R:
#    - Lower (0.5-1.0): Activates trailing early
#    - Medium (1.5-2.0): Balanced approach
#    - Higher (2.5+): Only trails after significant profit
#
# 4. COMBINING WITH BREAKEVEN:
#    Set breakeven to trigger before trailing for risk-free trades:
#    USE_BREAKEVEN=true
#    BREAKEVEN_TRIGGER_RR=1.0
#    TRAILING_STOP_TRIGGER_RR=1.5
#
# 5. TESTING:
#    Run: python test_atr_trailing_stop.py
#    To verify the implementation before live trading
#
# ============================================================

